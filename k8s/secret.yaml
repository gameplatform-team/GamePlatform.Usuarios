### NOTE: Este arquivo é apenas referência. O workflow cria/atualiza
### o Secret diretamente a partir dos GitHub Secrets para evitar
### commitar valores sensíveis no repositório.

apiVersion: v1
kind: Secret
metadata:
  name: gameplatform-usuarios-secret
type: Opaque
# Exemplo de chaves que o aplicativo espera. Não coloque valores reais aqui.
stringData:
  ConnectionStrings__DefaultConnection: "<DB_CONNECTION_STRING_PLACEHOLDER>"
  JWT__Secret: "<JWT_SECRET_PLACEHOLDER>"
